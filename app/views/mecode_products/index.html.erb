<h1>MecodeProducts#index</h1>
<h1><%= t "hello" %></h1>

<%= link_to "Login", new_session_path unless authenticated? %>
<%= link_to "New product", new_mecode_product_path if authenticated? %>
<div id="products">
  <% @products.each do |product| %>
    <div>            <%= link_to product.name, mecode_product_path(product.id) %>
    </div>
  <% end %>
</div>
<p>Find me in app/views/mecode_products/index.html.erb</p>
